<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huawei.micro.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.huawei.micro.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="status" property="status"/>
        <result column="deleted" property="deleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <resultMap id="RoleResultMap" type="com.huawei.micro.entity.Role">
        <id column="id" property="id"/>
        <result column="role_name" property="roleName"/>
        <result column="role_code" property="roleCode"/>
        <result column="description" property="description"/>
        <result column="deleted" property="deleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据用户名查询用户 -->
    <select id="selectUserByUsername" resultMap="UserResultMap">
        SELECT id, username, password, email, phone, status, deleted, create_time, update_time
        FROM sys_user
        WHERE username = #{username}
          AND deleted = 0
        LIMIT 1
    </select>

    <!-- 统计用户名数量（含逻辑删除用户，用于唯一性校验） -->
    <select id="countByUsername" resultType="int">
        SELECT COUNT(1)
        FROM sys_user
        WHERE username = #{username}
        <if test="excludeUserId != null">
            AND id != #{excludeUserId}
        </if>
    </select>

    <!-- 根据用户 ID 查询关联角色 -->
    <select id="selectRoleByUserId" resultMap="RoleResultMap">
        SELECT r.id, r.role_name, r.role_code, r.description, r.deleted, r.create_time, r.update_time
        FROM sys_role r
                 INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.deleted = 0
    </select>

</mapper>
